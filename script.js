const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const packingItems = await prisma.packingItem.findMany({ select: { brandName: true }, distinct: ['brandName'] }); for (const p of packingItems) { const exists = await prisma.product.findFirst({ where: { name: p.brandName, category: 'PACKAGING_MATERIAL' } }); if (!exists) { await prisma.product.create({ data: { name: p.brandName, category: 'PACKAGING_MATERIAL', unit: 'BAG' } }); console.log('Created product for', p.brandName); } } } main().finally(() => prisma.$disconnect());
